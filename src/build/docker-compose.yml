version: '3'
services:
  app:
    build: ..
    environment:
      - PORT=3001
    volumes:
      - ..:/usr/src/app
    command: npm start
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
